---
title: "Algorithm or Data Structure Name"
category: "Arrays & Strings"
# Other valid categories:
# Linked Lists | Stacks & Queues | Trees | Graphs | Dynamic Programming
# Backtracking | Binary Search | Heaps | Sorting | Math & Bits | Analysis

tags: [tag1, tag2]
# Tags are freeform — use terms someone might search for
# Examples: two-pointers, sliding-window, greedy, memoization, bfs, dfs

difficulty: "intermediate"
# foundational | intermediate | advanced

time_complexity: "O(n)"
space_complexity: "O(1)"
time_note: "One-line justification shown in the complexity table."
space_note: "One-line justification."
complexity_class: "good"
# good  → green  (O(1), O(log n), O(n))
# ok    → amber  (O(n log n))
# heavy → red    (O(n²) or worse)

date_added: 2026-07-16
# Format: YYYY-MM-DD

related: []
# Slugs of related entries, e.g.: ["sliding-window", "prefix-sum"]
# Slug = the filename without .md, e.g. "two-pointers" for two-pointers.md

leetcode_problems:
  - name: "Problem Name"
    number: 1
    difficulty: "medium"
    # easy | medium | hard
# Add more problems by duplicating the block above

svg_type: "array"
# array | linked-list | tree | graph | heap | stack | queue | none
# This generates the diagram shown in the entry header automatically
---

## Intuition

Explain the core idea in 2–3 paragraphs. Write for someone who already knows
how to code and wants to understand *why* this works, not just *how* to implement it.

Use math inline with $single dollar signs$ when it helps clarity.

$$
\text{Use double dollars for block equations}
$$

## Formal Definition

Give a precise definition. Use mathematical notation where appropriate.

## When to Use It

- **Pattern 1:** Describe what the input/problem looks like when this is the right approach.
- **Pattern 2:** Another recognition pattern.
- **Pattern 3:** Edge case or variant where this still applies.

## Complexity Analysis

**Time: `O(?)`** — One or two sentences explaining *why*, not just *what*.

**Space: `O(?)`** — Same — justify the bound.

## Pseudocode

```pseudo
ALGORITHM Name(input):
    // Step 1: initialize
    // Step 2: main loop
    // Step 3: return
```

## Implementation

### Python

```python
def algorithm_name(input):
    # Explain what this line does
    pass
```

### Java

```java
public ReturnType algorithmName(InputType input) {
    // Explain what this line does
}
```

## Worked Example

**Problem:** State a concrete problem.

**Input:** `example_input`
**Expected output:** `expected_output`

**Step-by-step trace:**

| Step | State | Action |
|------|-------|--------|
| 1    | ...   | ...    |

**Solution:**

```python
# Complete solution with comments
```

## Common Mistakes

- **Mistake 1:** What goes wrong and why.
- **Mistake 2:** Another frequent error.
